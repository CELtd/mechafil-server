# Copy this file to .env and fill in your credentials
# Either provide a bearer token or a path to an auth JSON file
# The JSON file should look like: { "auth_key": "Bearer YOUR_TOKEN_HERE" }

# Spacescope API Authentication (required)
# SPACESCOPE_TOKEN=Bearer YOUR_TOKEN_HERE
# SPACESCOPE_AUTH_FILE=../auths/spacescope_auth.json

# Server Configuration (optional)
# HOST=0.0.0.0
# PORT=8000
# LOG_LEVEL=INFO

# Data Refresh Scheduler (optional)
# RELOAD_TRIGGER=02:00        # Daily refresh time in UTC (HH:MM format)
# RELOAD_TEST_MODE=false      # Set to true for 2-minute refresh cycles (testing only)
